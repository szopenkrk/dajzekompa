{
    "name": "projektmiasto",
    "version": "0.1.0",
    "description": "Projekt Miasto",
    "main": "index.js",
    "scripts": {
        "build": "npm run clean && tsc -p .",
        "clean": "rm -rf build/",
        "dev": "nodemon -e ts,json --exec \"npm run start\"",
        "package": "cd build && tar -zcf ../artifact.tar.gz . && cd ..",
        "start": "npm run build && node -r dotenv/config build/server/index.js dotenv_config_path=env-local.env",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "repository": {
        "type": "git",
        "url": "git+https://github.com/szopenkrk/projektMiasto.git"
    },
    "keywords": [
        "projekt",
        "miasto"
    ],
    "author": "",
    "license": "UNLICENSED",
    "bugs": {
        "url": "https://github.com/szopenkrk/projektMiasto/issues"
    },
    "homepage": "https://github.com/szopenkrk/projektMiasto#readme",
    "devDependencies": {
        "dotenv": "^8.2.0",
        "nodemon": "^2.0.2",
        "typescript": "^3.8.3"
    },
    "dependencies": {
        "body-parser": "^1.19.0",
        "express": "^4.17.1"
    }
}
